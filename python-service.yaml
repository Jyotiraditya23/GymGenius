apiVersion: v1
kind: Service
metadata:
  name: python-service
spec:
  selector:
    app: python-microservice
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30010
  type: NodePort